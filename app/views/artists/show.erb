<h1>Artist Information Page</h1>


    <h2>Artist Name: <%=@artist.name%></h2>
    <h3>Songs:<br></h3>
    <%@artist.songs.each do |song|%>
      <ul>
        <li><a href="/songs/<%=song.slug%>"><%=song.name%></a></li>
      </ul>
    <%end%>
    <h3>Genres:<br></h3>
    <%@artist.genres.each do |genre|%>
      <ul>
        <li><a href="/genres/<%=genre.slug%>"><%=genre.name%></a></li>
      </ul>
    <%end%>
